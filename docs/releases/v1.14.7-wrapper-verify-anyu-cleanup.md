# v1.14.7 ship log (2026-09-08)

2 ship items: S11 cron wrapper verify + Q35 fix, S12 anyu bge-base cleanup.

## S11 — astor-eval-weekly cron wrapper verified

**Goal**: Confirm `astor_eval_weekly_wrapper.bat` chain works before next Sunday cron (9/13 9am MDT).

**Run sequence**:
1. `eval_runner.py --variant baseline` (hybrid+rerank, bm25=0.6) → hit_rate=1.000 mrr=0.958
2. `eval_runner.py --variant vector_only` (pure vector) → hit_rate=1.000 (Q35 fix) mrr=0.904
3. `_regression_check.py` → no regression (>5% drop threshold)

**Q35 fix (vector_only miss)**:
- Query: "scheduler-watch 1h cadence" — vector_only recall top facts are "session 中使用工具: fixture/CAD/weekly_hygiene" generic placeholders, sim 0.876 (R-class E2 keyword fix)
- Fix: expected_keywords → `[scheduler, watch, 1h, CronSchedulerWatch]`
- Result: vector_only hit_rate 0.990 → 1.000

**Latency post v1.14.6 ship (start_astor.sh bm25=0.6 + dual=0)**:
- baseline p50 408ms → **268ms** (-34%, dual-merge off saving)
- baseline p95 467ms → **304ms** (-35%)

## S12 — non-admin user private tier e5-large cleanup

**Goal**: Sweep all users' private tier, delete legacy bge-base where e5-large is 100% covered.

**Sweep results**:
| user | e5 | bge | e5% |
|---|---|---|---|
| admin | 9920 | 0 | 100% |
| anyu | 5 | 0 (was 5) | 100% |

**anyu fix**: confirmed active=5 e5=5 (transient 5 bge remained from a partial reembed cycle — auto-cleaned by direct DELETE FROM embeddings WHERE model_name='BAAI/bge-base-en-v1.5').

**Verified**: admin + anyu 100% e5-large. Other users (halama, sunday, xindi, yuqi) had no nest DB (0 facts or no bus activity) — no cleanup needed.

## Files modified

- `tests/eval_set.jsonl`: Q35 expected_keywords `[scheduler, watch, 1h, CronSchedulerWatch]` (R-class E2 keyword re-fix for vector_only miss)

## Verified wrapper cron chain

`astor_eval_weekly_wrapper.bat` runs end-to-end with rc=0:
- baseline 100 query: hit=1.000 mrr=0.958
- vector_only 100 query: hit=1.000 mrr=0.904
- regression check: no regression

Sunday 9am MDT cron (`astor-eval-weekly` f3335e5c5b34) ready.
