# v1.14.9 ship log (2026-09-08): S21 — hermes zombie session cleanup

**Root cause**: `<admin>(<platform_a>)` showed token consumption in slash_status cron even though admin no longer uses Feishu. Investigation revealed a 50-day-old zombie session (`20260720_173920_f3f179d0`, started 2026-07-21, `ended_at IS NULL`) that accumulated 61k input + 571k cache_read tokens. With 61 zombie sessions >7d active (admin fs + <admin>(<platform_b>) + various users + cron + cli + subagent), the slash_status cron 24h window + `ended_at IS NULL` filter always matched these stale rows.

**Ship**:
1. `clean_zombie_sessions.py` (~/<user>\AppData\Local\hermes\scripts\) — closes sessions with `ended_at IS NULL AND started_at < (now - 7d)`. Default `--days=7`, supports `--dry-run`.
2. Cron job `astor-cleanup-zombie-sessions-weekly` (id 5423ee50a6fe) — schedule `0 4 * * 0` (Sun 04:00 UTC = Sat 22:00 MDT), runs `clean_zombie_sessions.py` via `no-agent` mode.
3. Manual cleanup: closed 45 zombie sessions >7d (1 admin fs, 7 <admin>(<platform_b>), 4 cron, 6 subagent, 27 cli/system).

**R-class U (locked 2026-09-08)**: stale sessions (ended_at IS NULL AND started_at < now-7d) MUST be auto-closed weekly. Without this, slash_status cron keeps showing ghost token usage indefinitely, because the SQL filter `started_at > yesterday OR ended_at IS NULL` always matches zombies.

**Verified**: clean_zombie_sessions.py dry-run shows 45 sessions, manual close succeeded, hermes cron run succeeded. Next auto-run Sun 04:00 UTC.
