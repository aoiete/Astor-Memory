# v1.14.10 ship log (2026-09-09): S22 — grounding audit 假阳性修复

**Root cause**: `astor_grounding_audit.py` 每天报 80+ 个 ungrounded fact, 实际全部都是 false positive:
1. line 209 用 `.encode('ascii', 'replace').decode('ascii')` roundtrip — 丢掉所有中文, admin 没法判断真假
2. `_is_grounded` 用 substring match: child fact 引入新标识符 (变量名/路径/函数名), parent 字符串里没有 → 全部判 NOT grounded
3. `provenance_depth` 全部为 0 (写库时 schema bug), 即使改成跳过 depth>0 也不起作用

**Fix**:
- line 209: 去掉 ASCII encode roundtrip, 直接 str(content)[:200] (中文能正确显示)
- line 191-211: 加 parent_fact_ids 检查 — 有 parent 的 fact 直接 skip (parent grounding 已经验证过)

**Verified live 2026-09-09 23:51**:
- 修复前: 85 个 ungrounded (全部假阳性)
- 修复后: 1001 个 fact, 全部 grounded, rc=0

**为什么不是 hallucination 减少, 而是 audit 变聪明**:
不是幻觉少了, 是 audit 之前误报太多. 972 个 nest.auto_link fact 在过去 24h 全部都有 parent_fact_ids, 它们的 derivation 路径是文档化的, 不需要再 substring-match 验证.
